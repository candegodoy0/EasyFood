<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- imagen de fondo que ocupa toda la pantalla -->
    <ImageView
        android:id="@+id/imgPortada"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:src="@drawable/portada_inicio"
        android:contentDescription="@string/imagen_portada" />

    <!-- degradado oscuro sobre la imagen para que se vea mejor el texto -->
    <View
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@android:drawable/screen_background_dark_transparent" />

    <!-- contenido que va encima de la imagen y el degradado -->
    <LinearLayout
        android:orientation="vertical"
        android:gravity="bottom|center_horizontal"
        android:padding="24dp"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- espacio arriba para empujar el contenido hacia abajo -->
        <View
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1" />

        <!-- titulo grande de la app -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="EasyFood"
            android:textSize="28sp"
            android:textStyle="bold"
            android:textColor="@android:color/white"
            android:layout_marginBottom="8dp" />

        <!-- parrafo explicando la app resumido -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Easyfood te ayuda en la cocina busca recetas agrega recibe sugerencias al azar y encuentra que preparar con lo que tengas en casa guarda tus favoritas y listo"
            android:textColor="@android:color/white"
            android:textSize="14sp"
            android:gravity="center"
            android:layout_marginBottom="24dp" />

        <!-- boton para comenzar a usar la app -->
        <Button
            android:id="@+id/btnComenzar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/comenzar"
            android:backgroundTint="@color/amarilloBoton"
            android:textStyle="bold"
            android:textAllCaps="false"/>
    </LinearLayout>
</FrameLayout>
